# Replicate API Configuration
VITE_REPLICATE_API_TOKEN=your_replicate_api_token_here

# Note: In production, API calls should be proxied through your backend
# to keep API keys secure and avoid CORS issues